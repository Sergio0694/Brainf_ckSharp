<UserControl
    x:Class="Brainf_ckSharp.UWP.Controls.Host.InputPanel.MemoryViewer.CompactMemoryViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Brainf_ckSharp.UWP.Controls.Windows.UI.Xaml.Controls"
    xmlns:dataTemplates="using:Brainf_ckSharp.UWP.Controls.DataTemplates"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!--Memory cells grid-->
    <controls:FluentGridView
        ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}"
        SelectionMode="None"
        IsHitTestVisible="False">
        <controls:FluentGridView.Header>
            <Grid Height="12"/>
        </controls:FluentGridView.Header>
        <controls:FluentGridView.Footer>
            <Grid Height="12"/>
        </controls:FluentGridView.Footer>
        <controls:FluentGridView.ItemTemplate>
            <DataTemplate>
                <dataTemplates:Brainf_ckMemoryCellChunkTemplate Width="240" Height="60"/>
            </DataTemplate>
        </controls:FluentGridView.ItemTemplate>
    </controls:FluentGridView>
</UserControl>
